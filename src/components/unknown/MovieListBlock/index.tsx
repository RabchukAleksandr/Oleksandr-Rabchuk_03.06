import React from 'react';

type MovieListBlockProps = {}
const MovieListBlock: React.FC<MovieListBlockProps> = () => {
    return (
        <div>

        </div>
    );
};

export default MovieListBlock;